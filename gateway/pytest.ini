[pytest]
asyncio_mode = auto
# 必须让夹具与测试共用同一个 event loop：调度器与事件转发任务是在
# runtime 夹具里创建的后台任务，换成 function 级 loop 就永远不会被调度。
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
